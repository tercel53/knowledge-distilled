# Knowledge Distilled — Agent 工作规范

## 项目目标

将外部技术文章整理为可快速理解的知识笔记。保持来源可追溯，区分原文事实、阅读推论与本项目应用建议。同步维护 HTML、Markdown 和 metadata.json。

## 参考图转可编辑图解：视觉保真是验收条件

当用户认可参考图，并要求用 HTML、CSS、JavaScript/TypeScript、SVG 实现时，目标是忠实还原其视觉特征，同时实现可编辑性。不能仅保留节点关系后自由重设计。

### 实现前

1. 打开并确认用户选定的原图；不要依赖记忆或只读文件名。
2. 明确比较范围：一张关系图、一个组件或整页。组件图不应与整页截图直接比较。
3. 记录画布尺寸、节点位置与比例、对齐、字体层级、颜色、边框、圆角、图标和箭头走向。
4. 区分不应改变的视觉特征与允许增加的交互。未经用户要求，不删除图标、不弱化重点、不改标题或替换整体排布。

### 实现方式

- 结构化关系图优先使用语义化 HTML、CSS 和 SVG；装饰插画可保留生成图片。
- 节点文字、说明和关系应能在代码或数据配置中修改；不要将文字烘焙进位图来冒充可编辑实现。
- 图标优先使用风格接近的成熟图标库，保存来源与许可证；不要用 emoji 或无关符号代替。
- 桌面使用参考画布比例、网格或相对位置。连接线应根据布局定位，不能穿过文字和节点主体。
- 响应式重排可以改变移动端排列方式，但必须保留节点含义、主次和反馈方向；不应以手机适配为由改变桌面参考设计。
- 保持项目现有静态运行方式；只有确有需要时才增加 TypeScript 编译或其他依赖。

### 必须完成的视觉验收

1. 使用 image-to-code 实现流程及 design-qa 对照流程（如当前环境可用）；没有技能时仍执行以下检查。
2. 浏览器渲染实际页面，截取与参考图相同的组件区域。
3. 统一尺寸、比例、设备密度、主题与交互状态。先消除截图缩放和浏览器外框造成的伪差异。
4. 在同一比较输入中查看原图和实现图，逐项检查：
   - 字体、字号、字重、换行与层级；
   - 节点比例、位置、内外间距；
   - 色彩、边框、圆角、背景；
   - 图标形态和存在性、线宽、箭头与反馈回路；
   - 文案、语义、交互和可访问性。
5. 对明显偏差先修复，再截图并再次比较。页面能打开、没有报错、功能正常，均不能代替视觉验收。
6. 至少验证桌面及 390px 手机宽度，必要时检查断点附近；确认没有横向溢出、遮挡、错误连线，点击和键盘焦点有效。
7. 在 `docs/design/<主题>/` 保存参考、实现截图和验收报告。报告注明修正历史、已验证事项、残余差异，禁止无证据宣称“像素级一致”。
8. 重大视觉偏差未修复时，不应宣称完成。确实无法比较时应明确报告验证受阻。

### 本仓库案例

Harness 图参考为 `ai/agents/openai/harness-engineering/overview.png`；实现为同目录的 `index.html`、`note.css`、`note.js`。对照记录见 `docs/design/harness/qa.md`。

此前失误：保留关系但删除图标、改宽节点、弱化线条及反馈栏，只验证交互后交付。以后必须通过原图与代码渲染的同尺寸比较来避免此类漂移。

## 交付与提交

简要说明修改、验证结果与已知差异。遵循用户授权范围提交或推送；不要把临时机器路径、浏览器会话信息和凭证纳入仓库。
