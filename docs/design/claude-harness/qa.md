# Claude Harness 笔记验收

日期：2026-09-12

## 范围与来源

新建文章蒸馏页，复用知识库配色与静态样式。原文为 [Anthropic 文章](https://claude.com/blog/harnessing-claudes-intelligence)，作者 Lance Martin，发布于 2026-04-02。读取正文编号 1–3 节、末节和作者信息，排除导航与营销模块。

没有用户选定的视觉参考；两张图是阅读归纳，不以原文插图为像素基准。图标复用 assets/icons 中的 Bootstrap Icons，许可证保留在同目录。

## 截图与检查

- [1440px 桌面](1440.png)：实际浏览器整页渲染；目录、职责分层、横向数据流程和证据展示清晰，无可见遮挡或穿字连线。
- [390px 手机](390.png)：实际浏览器整页渲染；目录换行，数据流程纵排、箭头向下；分层图文字保留可读字号，无可见遮挡。
- [800px](800.png)、[801px](801.png)：断点两侧执行溢出检查，均未发现横向溢出；已留截图，未逐项人工视觉审查。
- 四个宽度均无重复 ID、缺失页内 hash 目标、HTTP 资源错误或页面脚本错误。
- 验证图中“编排动作”跳到 #delegate。
- 验证直接进入 #source-2 自动展开原文定位；summary 点击可折叠，键盘 Enter 可展开。
- HTML、Markdown、metadata 的核心结论、三项机制与来源对应；数字为原文报告的 Opus 4.6 / BrowseComp 过滤工具输出实验，不是本站复现实验。

## 修正与限制

首版浏览器检查未发现需要修正的明显视觉问题；最后仅整理源码换行便于编辑。未引入构建依赖。未进行原文基准复现；不宣称像素级一致或跨浏览器全覆盖。原文入口定位到整篇文章，笔记中通过编号章节说明依据位置。

## Markdown 中文编码修正

用户反馈直接打开 README.md 出现乱码。文件内容为 UTF-8，但本地服务响应为 `Content-Type: text/markdown`，未声明 charset。为该 Markdown 添加 UTF-8 BOM，让浏览器识别编码，正文未改变。

用中文 locale 的 Chromium 访问原 README.md 地址：HTTP 200，document.characterSet 为 UTF-8，标题文字匹配。截图 [Markdown 中文显示](markdown-encoding.png) 已确认中文正常。此截图仅检查编码；浏览器原始文本视图保留 Markdown 源码显示。

## 原文标题对齐

按用户确认，将主标题改为“Agent Harness 设计：释放 Claude 智能的三种模式”，下方展示英文原标题。HTML 页面标题、Markdown 一级标题和 metadata.title 同步；Markdown 的 UTF-8 BOM 保留。

重新检查 1440 / 390 / 800 / 801px，均无横向溢出、无缺失锚点或脚本/资源错误，节点跳转及折叠、键盘展开通过。目视核对桌面和手机截图：手机初次标题使“设计”拆行，调整手机字号为 32px 后重新截图，标题换行正常。现有尺寸截图已更新；早先 markdown-encoding.png 仅记录编码修复历史。

文章蒸馏技能加入标题规则；人工检查 frontmatter 未改变，新增规则保留用户显式改题的例外，引用路径未改变。quick_validate.py 因环境缺少 PyYAML 未能运行。

## 论证结构重组（当前版本）

用户指出旧页面仅呈现职责与工具，无法读出原文的推导。当前版本替换静态职责图，按“假设可能过时—利用已有能力—检验可下放的决策—说明保留项—升级后再次重测”组织阅读；明确三种模式不是固定实施顺序。

内容对照：

| 当前主张 | 原文依据 | 论证作用与限制 |
| --- | --- | --- |
| Harness 的能力假设会过时 | 引言、末节 | 将设计问题与模型变化相连 |
| 熟悉的工具可由模型组合 | 第 1 节 | 说明利用已有能力的基础；不推导取消安全边界 |
| 让模型通过代码处理工具输出 | 第 2 节动作编排讨论 | 保留旧假设、数据成本、替代方式及 Opus 4.6 / BrowseComp 条件 |
| 按需加载、清理、隔离当前上下文 | 第 2 节上下文讨论 | 区分 Skills、上下文编辑、子代理的作用 |
| 模型选择保留与读取的内容 | 第 2 节长期记忆讨论 | 压缩、记忆文件与游戏案例；不声称检索设施在所有任务中无用 |
| 保留成本、交互与安全支撑 | 第 3 节 | 补回这些机制为何需要，避免工具名堆叠 |
| 模型升级后重新评估补偿 | 末节 | 上下文重置案例回应开篇，而非只作为边界说明 |

截图：restructured-1440.png、restructured-390.png、restructured-800.png、restructured-801.png 为本轮整页截图；overview-1440.png、overview-390.png、delegate-1440.png、delegate-390.png 为组件截图。此前无 restructured 前缀的整页图为历史版本。

目视检查桌面整页、桌面总览、手机总览和手机核心论证区域：层级可读，无可见遮挡；手机总览将两类理由分组纵排，工具数据流向下，未缩小整张桌面图。四个宽度的溢出、重复 ID、缺失 hash 目标、资源和脚本错误检查均通过。图节点跳转、原文定位深链接自动展开、键盘 Enter 展开通过；Markdown 为 UTF-8 且中文正文匹配。标题、核心结论、metadata 大纲和图解 ID、本地资源校验通过。

未进行跨浏览器全覆盖或原文实验复现。本轮图是论证归纳，非原文插图复刻。HTML、Markdown 和元数据同步，Markdown BOM 保留。

## 图形优先重构（当前版本）

用户确认内容正确，要求减少依赖段落解释。本轮保留核心主张、论证、数字、限定与原文依据，将详细解释移入 7 处可展开区域，主阅读路径由图形表达。

### 图形职责

- 总览：保留能力变化触发重测及两类设计理由的分支。
- 通用工具：Bash 与文件编辑的共同底座，分支到可组合行为。
- 工具编排：以相同表格和高亮列对照“全部回传”与“先筛选再回传”，不是只画抽象文字框。
- 当前上下文：完整指令页与索引/选中正文对照，辅以加载、移除、隔离。
- 长期记忆：当前与后续窗口之间并列的压缩、记忆文件通路，不暗示必须串行执行。
- 缓存：两轮请求对齐，稳定前缀保持一致、变化部分追加。
- 动作边界：模型提出动作，Harness 通过专用工具承接按需检查，再执行。
- 升级案例：Sonnet 4.5 的补偿与 Opus 4.5 中补偿理由失效的对照，返回总览。

图解基于已确认内容新设计，没有用户指定的图片保真基准。表格与缓存宽度为示意，非定量比例；图标复用仓库 Bootstrap Icons 及现有许可证。节点与文字均为 HTML/CSS，可直接修改。

### 实际检查与修正

当前截图以 visual- 开头：1440 / 390 / 800 / 801 为整页，tools / orchestration / context / memory / boundaries / limits 为桌面与手机组件截图。之前 restructured- 截图属于历史文字展开版。

目视检查全部六类新组件的桌面与 390px 截图，节点可读、无明显遮挡。首次检查发现手机记忆图缺少明确的跨窗口连线，已补齐上下两侧箭头，桌面保持左右箭头；重新截图确认。修正了工具组合分支横线与节点中心的对齐。手机表格对照保留可辨认单元格，较长流程改为纵排。

四个宽度均无横向溢出、重复 ID、缺失 hash 目标、资源请求错误或页面脚本错误。7 处解释区域的键盘 Enter 展开均通过；全部展开后手机仍无横向溢出，展开截图为 visual-context-expanded-390.png。证据链接自动展开原文定位，图节点跳转通过。Markdown 仍为 UTF-8，中文匹配；八幅图的 ID、元数据、Markdown 图解链接和本地资源一致。

限制：未做跨浏览器全覆盖；图解表达原文机制和阅读归纳，未复现实验。图形增加了页面长度，默认阅读仅展开图示，细节按需查看。
