# Harness 图解视觉验收

final result: passed

## 对照目标与证据

- 原始视觉依据：`../../../ai/agents/openai/harness-engineering/overview.png`，1536 × 1024。
- 同尺寸参考：`reference.png`，1344 × 896。
- 实现截图：`implementation.png`，1344 × 896；仅截取 #harness-map，不包含文章外框。
- 浏览器桌面 viewport：1691 × 1800 CSS px。图组件实际 1344 × 896 CSS px。
- 截图工具输出 1676 × 1784 px；先恢复到 CSS viewport 尺寸，再按组件位置 (166, 553) 裁切。原图等比缩放到组件大小。
- 初始交互状态：知识模块选中；选中描边是交互增强。
- 桌面参考和实现通过同一工具输入共同展示后对照；全尺寸可读，无需额外文字局部裁切。
- 手机 viewport：390 × 844；内容宽度 375（含浏览器滚动条），无横向溢出。

## 修正历史

1. 上版存在 P1 视觉漂移：图标缺失、宽节点重排、标题弱化、细线和反馈文字栏缺少视觉重点。
2. 按原图 3:2 画布恢复相对坐标、蓝色描边、七个官方 SVG 图标、标题与实心反馈栏。
3. 首轮浏览器检查发现 P2 箭头过大、内部连线过陡；缩小 marker，修正与 Agent 的连接点，并调整标题字号和工具图标方向。
4. 重新截图比较：主要节点位置、比例、布局与反馈路径恢复；无待修复 P0/P1/P2。

## 五项视觉检查

- 字体：标题、节点和辅助文字的层级已恢复；系统字体与生成图的字形仍有细微差异。
- 布局：桌面遵循原图节点位置；手机有意改为纵向，维持同一关系和反馈方向。
- 颜色：钴蓝强调、浅蓝容器和深色正文；原图的轻微纹理/渐变以纯色表达。
- 图像/图标：Bootstrap Icons 官方 SVG，保留 LICENSE；角色与视觉重量接近，机器人、扳手、书籍等具体造型并非原图逐笔描摹。
- 内容：恢复原图标题、节点和底部说明；组件外附点击解读，不占用参考画布。

## 功能验证

- 模块点击更新解读与选中状态。
- 390px 下无横向溢出，SVG 路径无 NaN。
- 连接线随尺寸更新；手机反馈回路绕过交付节点。
- 浏览器错误日志为空。
- 图标全部本地加载，无运行时 CDN 依赖。

## 残余 P3 / 取舍

- 图标库形状、字体字形、细微光影与原图不同，不声称像素级一致。
- 手机采用纵向排列，因此不与原图横向画布逐像素比较。
