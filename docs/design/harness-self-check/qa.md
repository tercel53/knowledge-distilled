# 静态自评试点验收

日期：2026-09-12。范围：skill 与配套规则、文章模板、Harness 初读定位和自评区。未提交或发布。

## 修改与修正

- 默认支持探索式阅读，补充重要省略说明；自评细则放入独立参考文件。
- 旧八段模板更新为可调整的递进阅读骨架，包含原生折叠答案示例；不强制应用、知识关联或固定观点数。
- Harness 增加四题，分别对应知识可用性、规则执行、观察反馈与结论边界；原有总览和三张机制图结构保留。
- 第 2 题需要的修正、重查及边界约束说明已在正文补足；Markdown 同步相同解释和条件边界。
- 修改前结构检查发现导航/眉题命名漂移，以及校验器未识别 header、article 与 h3。已统一导航用词并修正解析范围；没有放宽链接或标题一致性检查。

## 内容与结构验证

- `quick_validate.py`：通过；使用临时依赖环境提供 PyYAML，未新增项目依赖。
- `validate_article.py ai/agents/openai/harness-engineering`：通过，40 个 ID、10 个导航标签、11 个大纲条目。
- 校验器回归：嵌套 header/article/h3 合法结构通过；错误导航标签、失效锚点、metadata 标题漂移均被拒绝。
- 实例化模板：4 个章节的导航、metadata、资源及自评回查链接通过校验。
- HTML 自评区文本与 Markdown 对照：四题、参考解释、对照要点、误解提示及原文章节定位一致。
- 题目依据与正文位置见 [content-plan.md](content-plan.md)。独立审读另见 [independent-review.md](independent-review.md)。

## 生成者浏览器自检

- 桌面 1440×1000：四道问题直接可见，初始答案状态均为关闭；页面无横向溢出。
- Enter 展开第 1 题后，焦点在 summary 且有可见轮廓，其他三题保持关闭。
- 390×844：展开后的参考解释、对照点、误解提示与英文来源章节正常换行；页面无横向溢出，未见遮挡。
- 截图：[桌面折叠](desktop-closed.png)、[桌面展开与键盘焦点](desktop-open.png)、[手机展开](mobile-open.png)。
- 原生交互的无脚本检查使用同一文章的临时预览服务，以 `Content-Security-Policy: script-src 'none'` 禁止脚本。响应头已核实；独立浏览器操作结果见独立报告。该服务配置不进入网站产品。

## 最后修正与补充检查

- 独立验收提出第 4 题的原文章节定位可更完整，已补充空仓库、自主程度和持续清理章节，同步 HTML/Markdown；定向复验见独立报告。
- 生成者另以 Enter 激活第 3 题“观察反馈”回查，待页面滚动稳定后读取实际位置：目标顶部约 52px，机制标题、图和正文落入视口。见 [键盘回查](keyboard-return.png)。这补充了独立报告中未完整验证的键盘滚动落点，但不冒充独立验收者的观察。

## 已知范围

本轮没有重绘图解，也没有重新执行历史参考图的保真比较。模板进行了结构实例化检查；浏览器展示验收针对实际 Harness 样例。Markdown 已核对折叠语法与内容，未发布到 GitHub 验证远端渲染。不以本次代理验收或自评材料证明真实读者理解速度、掌握比例或长期工程能力。
